name: Test GitHub App

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-24.04-riscv

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Test GitHub App webhook
        run: |
          echo "Repository: $GITHUB_REPOSITORY"
          echo "SHA: $GITHUB_SHA"
          echo "Event: $GITHUB_EVENT_NAME"
